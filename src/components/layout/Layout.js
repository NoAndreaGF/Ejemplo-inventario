import HeaderNavbar from '../header/HeaderNavbar';
import Menu from '../menu/Menu';

function Layout() {
    return (
        <div>
            <Menu />
        </div>
    );
}

export default Layout;